#include <string.h>

/**
 * @brief checks for flags when starting a program
 * 
 * @param [in] x number of flags
 * @param [in] y list of flags
 * @param [in] y flag
 * 
 * @return if flag in the flag was entered
 */
bool check_flag(int argc, char *argv[], const char *flag);